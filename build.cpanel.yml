---
deployment:
  tasks:
    - export DEPLOYPATH=/home/example/public_html/
    - /bin/cp -R * $DEPLOYPATH
    - cd $DEPLOYPATH
    - echo "Installing dependencies..."
    - /bin/npm install
    - echo "Starting application..."
    - /bin/npm start
